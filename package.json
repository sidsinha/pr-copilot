{
  "name": "pr-copilot",
  "version": "1.0.0",
  "description": "AI-powered PR creation tools with GitHub integration and AI LLM support",
  "type": "module",
  "scripts": {
    "mcp": "node servers/mcp-server.js",
    "api": "node servers/api-server.js",
    "build": "npm run clean && npm run copy-files && npm run create-entrypoint",
    "clean": "rm -rf dist",
    "copy-files": "mkdir -p dist && cp -r servers dist/ && cp -r tools dist/ && cp -r config dist/ && cp package.json dist/",
    "create-entrypoint": "node scripts/entry.js",
    "start": "node dist/index.js",
    "dev": "npm run build && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@langchain/core": "^0.3.41",
    "@langchain/openai": "^0.5.16",
    "@modelcontextprotocol/sdk": "^0.5.0",
    "axios": "^1.10.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "zod": "^3.25.71"
  }
}
